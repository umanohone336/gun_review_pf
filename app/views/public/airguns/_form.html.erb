<%= form_with model: airgun, url: '/airguns',local:true do |f| %>
  <div class="form-group">
    <%= f.file_field :image, accept: "image/*" %>
  </div>
  <div class="form-group">
    <%= f.label :title %>
    <%= f.text_field :title, class: 'form-control airgun_title' %>
  </div>
  <div class="form-group">
    <%= f.label :opinion %>
    <%= f.text_area :body, class: 'form-control airgun_body' %>
  </div>
  <div class="form-group">
    <%= f.submit class: 'btn btn-success' %>
  </div>
<% end %>
<!--local:true 同期通信の関係-->
<!--url: '/airguns'これがないと今いるページにいく。-->
<!--GET POST（メソッド） と　URLの関係性を見るこれでアクションが決まる。-->