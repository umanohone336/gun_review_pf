<%= form_with(model:[airgun, comment], local: true) do |f| %>
  <%= f.text_area :comment_body, rows:'5',placeholder: "コメントをここに", class: "w-100" %>
  <%= f.submit "送信する", class: "btn btn-secondary pull-right" %>
<% end %>